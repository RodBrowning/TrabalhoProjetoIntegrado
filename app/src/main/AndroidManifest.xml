<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tesouradouradaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DadosEstabelecimentoActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".EditarEstabelecimentoActivity"
            android:parentActivityName=".DadosEstabelecimentoActivity" />
        <activity
            android:name=".ServicosActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AdicionarEditarServicoActivity"
            android:parentActivityName=".ServicosActivity" />
        <activity android:name=".AdicionarEditarAgendamentoActivity"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>